SOURCES = $(wildcard *.erl)
OUTDIR = ../../ebin
BEAMS = $(addprefix $(OUTDIR)/,$(SOURCES:.erl=.beam))

EFLAGS = -pa $(OUTDIR)

all: $(BEAMS)

$(OUTDIR)/%.beam: %.erl
	@ERLC@ -W $(EFLAGS) -o $(OUTDIR) $<

clean:
	rm -f $(BEAMS)

distclean:
	rm -f Makefile
